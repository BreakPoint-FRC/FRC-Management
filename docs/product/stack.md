# Stack

The stack chosen before development started. What actually shipped matches this
choice; see the README for how the workspaces are wired together.

| Part | Tool |
| --- | --- |
| Backend | TypeScript (Fastify) |
| Frontend | Next.js (App Router) |
| PWA | Next.js manifest + a hand-written service worker |
| Database | PostgreSQL (via Prisma) |
